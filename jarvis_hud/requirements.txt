# JARVIS HUD — Python 3.10–3.12 (MediaPipe does not support 3.13 yet)
fastapi>=0.115
uvicorn[standard]>=0.30
websockets>=12
opencv-python>=4.9
mediapipe>=0.10.14
anthropic>=0.116
httpx>=0.27
python-dotenv>=1.0

# Optional: object detection ("what am I holding") — heavy, pulls in torch.
# pip install ultralytics   and set JARVIS_ENABLE_OBJECTS=1
